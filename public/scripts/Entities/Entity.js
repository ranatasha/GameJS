/*export*/ class Entity {
    constructor(/*physicManager, soundManager*/) {
        this.pos_x= 0;  // координаты объекта
        this.pos_y= 0;
        this.size_x= 0; // размер объекта в пикселях
        this.size_y= 0;
        /*this.physicManager = physicManager;
        this.soundManager = soundManager;*/
    }
}